import { env } from 'cloudflare:workers';
export function config(){const e=env as unknown as Record<string,string|undefined>;return {openai:e.OPENAI_API_KEY||process.env.OPENAI_API_KEY,tmdb:e.TMDB_READ_ACCESS_TOKEN||process.env.TMDB_READ_ACCESS_TOKEN,model:e.OPENAI_MODEL||process.env.OPENAI_MODEL||'gpt-6-astra'};}
export class AppError extends Error{constructor(public code:string,message:string,public status=502){super(message)}}
export function errorResponse(e:unknown){const err=e instanceof DOMException&&e.name==='TimeoutError'?new AppError('TIMEOUT','Research took too long. Your current trail is safe; please try again.',504):e instanceof DOMException&&e.name==='AbortError'?new AppError('CANCELED','Research was canceled. Your trail is unchanged.',499):e instanceof AppError?e:new AppError('UPSTREAM_ERROR','The service could not finish this request. Please try again.');return Response.json({error:{code:err.code,message:err.message,retryable:err.status!==400&&err.status!==503}},{status:err.status});}
export function checkOrigin(r:Request){const origin=r.headers.get('origin');if(origin&&origin!==new URL(r.url).origin)throw new AppError('INVALID_ORIGIN','This request must come from CLOSEUP.',403);}
